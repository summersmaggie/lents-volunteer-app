<div class="container" id="calendar">
  <%= month_calendar events: @time_slots do |date, time_slots| %>
    <%= date.day %>

    <% time_slots.each do |time_slot| %>
      <div>
      <%= link_to time_slot.start_time %>
      </div>
    <% end %>
  <% end %>
</div>
